<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.leanper.androidallstyle">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.INSTALL_SHORTCUT" />
    <uses-permission android:name="android.permission.UNINSTALL_SHORTCUT" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 写入扩展存储，向扩展卡写入数据，用于写入缓存定位数据 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 读取缓存数据 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- 添加删除桌面快捷的权限 -->
    <uses-permission android:name="com.android.launcher.permission.INSTALL_SHORTCUT" />
    <uses-permission android:name="com.android.launcher.permission.UNINSTALL_SHORTCUT" />

    <application
        android:name=".main.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".main.MainActivity"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- 别名2 -->
     <!--   <activity-alias
            android:name=".NewActivity2"
            android:enabled="false"
            android:icon="@mipmap/radar_chart"
            android:label="Alias2"
            android:targetActivity=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>
        <activity-alias
            android:name=".NewActivity1"
            android:enabled="false"
            android:icon="@mipmap/medium_circle_fill"
            android:label="My Heart❤"
            android:targetActivity=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>
-->
        <activity android:name=".activity.loadstate.LoadStateActivity" />
        <activity android:name=".activity.loadstate.TestActivity" />
        <activity android:name=".activity.myview.LearnDrawViewActivity" />
        <activity android:name=".activity.ChangeIconActivity" />
        <activity android:name=".activity.SendShortcutActivity">
            <intent-filter>
                <action android:name="android.intent.action.CREATE_SHORTCUT" />
            </intent-filter>
        </activity>

        <!-- Intent received used to install shortcuts from other applications -->
        <!-- <receiver -->
        <!-- android:name="com.android.launcher2.InstallShortcutReceiver" -->
        <!-- android:permission="com.android.launcher.permission.INSTALL_SHORTCUT"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="com.android.launcher.action.INSTALL_SHORTCUT" /> -->
        <!-- </intent-filter> -->
        <!-- </receiver> -->
        <activity android:name=".activity.LoadState2Activity" />
        <activity android:name=".activity.PickViewActivity" />
        <activity android:name=".activity.AddLayoutActivity" />
        <activity android:name=".activity.TestaActivity" />
        <activity android:name=".activity.myview.ViewPathActivity" />
        <activity android:name=".activity.PerssionActivity"></activity>
    </application>

</manifest>